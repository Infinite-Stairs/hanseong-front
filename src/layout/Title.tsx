import styles from "./Title.module.css";

const Title = () => {
    return (
        <header className={styles.wrapper}>
            <h1 className={styles.title}>Do While (001)</h1>
            <h5 className={styles.semititle}>위대한 001</h5>
        </header>
    );
}

export default Title;